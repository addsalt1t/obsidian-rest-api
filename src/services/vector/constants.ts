/**
 * Vector service constants
 */

/** Maximum embedding cache size */
export const MAX_CACHE_SIZE = 500;

/** Default number of vector search results */
export const DEFAULT_VECTOR_LIMIT = 10;

/** Default similarity threshold for vector search */
export const DEFAULT_SIMILARITY_THRESHOLD = 0.1;

/** Default result count */
export const DEFAULT_LIMIT = 10;

/** Maximum result count */
export const MAX_LIMIT = 100;

/** Search excerpt window size */
export const EXCERPT_WINDOW = 50;

/** Vector search result build concurrency */
export const SEARCH_CONCURRENCY = 10;
